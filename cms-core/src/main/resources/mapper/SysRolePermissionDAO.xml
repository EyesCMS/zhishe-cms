<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fzu.zhishe.core.dao.SysRolePermissionDAO">

  <select id="listPermissionByRoleId" parameterType="java.lang.Integer"
    resultMap="edu.fzu.zhishe.cms.mapper.SysPermissionMapper.BaseResultMap">
    SELECT
      p.permission_code,
      p.permission_name
    FROM
      sys_role r,
      sys_role_permission_rel rp,
      sys_permission p
    WHERE
      r.id = #{id}
      AND r.id = rp.role_id
      AND rp.permission_id = p.id
  </select>

</mapper>
